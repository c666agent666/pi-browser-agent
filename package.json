{
  "name": "pi-browser-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "extension"
  ],
  "scripts": {
    "dev:server": "bun --watch packages/pi-agent-server/src/index.ts",
    "dev:extension": "cd extension && bun run dev",
    "build:extension": "cd extension && bun run build",
    "check": "bunx tsc --noEmit -p packages/pi-agent-server",
    "start": "bun packages/pi-agent-server/src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}